<UserControl x:Class="HipHipParquet.Views.QualityReviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:HipHipParquet.Controls"
             xmlns:vm="clr-namespace:HipHipParquet.ViewModels"
             xmlns:local="clr-namespace:HipHipParquet.Views"
             xmlns:conv="clr-namespace:HipHipParquet.Converters"
             Background="#FAFAFA">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <conv:StringToBrushConverter x:Key="StringToBrushConverter"/>
        
        <!-- Card style for sections -->
        <Style x:Key="SectionCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="12"/>
        </Style>
        
        <!-- Score bar style -->
        <Style x:Key="ScoreBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Maximum" Value="25"/>
            <Setter Property="Foreground" Value="#512BD4"/>
            <Setter Property="Background" Value="#E8E8E8"/>
            <Setter Property="Margin" Value="0,2,0,4"/>
        </Style>

        <!-- Small badge style -->
        <Style x:Key="BadgeText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Padding" Value="4,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Selectable text style: TextBox that looks and behaves like a TextBlock -->
        <Style x:Key="SelectableText" TargetType="TextBox">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="KeyboardNavigation.TabNavigation" Value="None"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="Cursor" Value="IBeam"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <ScrollViewer x:Name="PART_ContentHost" Focusable="False"
                                      HorizontalScrollBarVisibility="Disabled"
                                      VerticalScrollBarVisibility="Hidden"
                                      Background="Transparent"
                                      Padding="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#512BD4" Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="🔍 Quality Review" FontSize="16" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="11" Foreground="#CCCCCC" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Analyze" 
                            Command="{Binding AnalyzeCommand}" 
                            Background="White" Foreground="#512BD4" 
                            FontWeight="Bold" Padding="12,4" Margin="0,0,6,0"
                            BorderThickness="0" Cursor="Hand"
                            Visibility="{Binding IsAnalyzing, Converter={StaticResource InverseBoolToVis}}"/>
                    <Button Content="⏹ Cancel"
                            Command="{Binding CancelAnalysisCommand}"
                            Background="#C62828" Foreground="White"
                            FontWeight="Bold" Padding="12,4" Margin="0,0,6,0"
                            BorderThickness="0" Cursor="Hand"
                            Visibility="{Binding CanCancel, Converter={StaticResource BoolToVis}}"/>
                    <Button Content="📄 Export" 
                            Command="{Binding ExportHtmlReportCommand}" 
                            Background="Transparent" Foreground="White" 
                            FontWeight="SemiBold" Padding="8,4"
                            BorderBrush="White" BorderThickness="1" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Progress Bar -->
        <ProgressBar Grid.Row="0" VerticalAlignment="Bottom" Height="3"
                     Value="{Binding AnalysisProgress}" Maximum="100"
                     Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVis}}"
                     Foreground="#FFD700" Background="Transparent" BorderThickness="0"/>
        
        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="12">
            <StackPanel>
                
                <!-- Empty state -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource InverseBoolToVis}}">
                    <StackPanel HorizontalAlignment="Center" Margin="0,20">
                        <TextBlock FontSize="36" HorizontalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="📂"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasFile}" Value="True">
                                            <Setter Property="Text" Value="🔍"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="14" Foreground="Gray"
                                   HorizontalAlignment="Center" Margin="0,8,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="No file loaded"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasFile}" Value="True">
                                            <Setter Property="Text" Value="Ready to analyze"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock FontSize="11" Foreground="Gray"
                                   HorizontalAlignment="Center" Margin="0,4,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Text" Value="Open a data file to begin"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasFile}" Value="True">
                                            <Setter Property="Text" Value="Click Analyze to profile this file"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>
                
                <!-- File Summary Section -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="File Summary" FontWeight="Bold" FontSize="13" Margin="0,0,0,8"/>

                        <!-- File name + analyzed at -->
                        <TextBox Style="{StaticResource SelectableText}" FontSize="11" FontWeight="SemiBold"
                                 Text="{Binding FileProfile.FileName, Mode=OneWay}" TextWrapping="Wrap" Margin="0,0,0,2"/>
                        <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="#888"
                                 Text="{Binding AnalyzedAtDisplay, Mode=OneWay}" Margin="0,0,0,10"/>

                        <!-- Stat tiles: 3-column grid -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <!-- Rows -->
                            <Border Grid.Row="0" Grid.Column="0" Background="#F5F5F5" CornerRadius="4" Padding="8,6" Margin="0,0,4,4">
                                <StackPanel>
                                    <TextBox Style="{StaticResource SelectableText}" FontSize="14" FontWeight="Bold"
                                             Text="{Binding FileProfile.RowCount, StringFormat='{}{0:N0}', Mode=OneWay}"/>
                                    <TextBlock Text="Rows" FontSize="9" Foreground="#888" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Columns -->
                            <Border Grid.Row="0" Grid.Column="1" Background="#F5F5F5" CornerRadius="4" Padding="8,6" Margin="0,0,4,4">
                                <StackPanel>
                                    <TextBox Style="{StaticResource SelectableText}" FontSize="14" FontWeight="Bold"
                                             Text="{Binding FileProfile.ColumnCount, Mode=OneWay}"/>
                                    <TextBlock Text="Columns" FontSize="9" Foreground="#888" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- File Size -->
                            <Border Grid.Row="0" Grid.Column="2" Background="#F5F5F5" CornerRadius="4" Padding="8,6" Margin="0,0,0,4">
                                <StackPanel>
                                    <TextBox Style="{StaticResource SelectableText}" FontSize="14" FontWeight="Bold"
                                             Text="{Binding FileProfile.FileSizeFormatted, Mode=OneWay}"/>
                                    <TextBlock Text="File Size" FontSize="9" Foreground="#888" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Format -->
                            <Border Grid.Row="1" Grid.Column="0" Background="#F5F5F5" CornerRadius="4" Padding="8,6" Margin="0,0,4,0">
                                <StackPanel>
                                    <TextBox Style="{StaticResource SelectableText}" FontSize="14" FontWeight="Bold"
                                             Text="{Binding FormatDisplay, Mode=OneWay}"/>
                                    <TextBlock Text="Format" FontSize="9" Foreground="#888" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Completeness -->
                            <Border Grid.Row="1" Grid.Column="1" Background="#F5F5F5" CornerRadius="4" Padding="8,6" Margin="0,0,4,0">
                                <StackPanel>
                                    <TextBox Style="{StaticResource SelectableText}" FontSize="14" FontWeight="Bold"
                                             Text="{Binding FileProfile.OverallCompleteness, StringFormat='{}{0:F1}%', Mode=OneWay}"/>
                                    <TextBlock Text="Completeness" FontSize="9" Foreground="#888" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Columns with Nulls -->
                            <Border Grid.Row="1" Grid.Column="2" Background="#F5F5F5" CornerRadius="4" Padding="8,6" Margin="0,0,0,0">
                                <StackPanel>
                                    <TextBox Style="{StaticResource SelectableText}" FontSize="14" FontWeight="Bold"
                                             Text="{Binding FileProfile.ColumnsWithNulls, Mode=OneWay}"/>
                                    <TextBlock Text="Cols w/ Nulls" FontSize="9" Foreground="#888" Margin="0,1,0,0"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Quality Score Gauge Section -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="Data Quality Score" FontWeight="Bold" FontSize="13" Margin="0,0,0,6"/>
                        
                        <controls:QualityGaugeControl HorizontalAlignment="Center"
                            Score="{Binding OverallScore}"
                            Grade="{Binding OverallGrade}"/>
                        
                        <!-- Component scores -->
                        <Grid Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,6,6">
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="Gray"
                                         Text="{Binding CompletenessScore, StringFormat='Completeness {0:F1}/25', Mode=OneWay}"/>
                                <ProgressBar Style="{StaticResource ScoreBar}" Value="{Binding CompletenessScore}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="6,0,0,6">
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="Gray"
                                         Text="{Binding UniquenessScore, StringFormat='Uniqueness {0:F1}/25', Mode=OneWay}"/>
                                <ProgressBar Style="{StaticResource ScoreBar}" Value="{Binding UniquenessScore}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,6,0">
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="Gray"
                                         Text="{Binding ValidityScore, StringFormat='Validity {0:F1}/25', Mode=OneWay}"/>
                                <ProgressBar Style="{StaticResource ScoreBar}" Value="{Binding ValidityScore}"/>
                            </StackPanel>
                            
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="6,0,0,0">
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="Gray"
                                         Text="{Binding DistributionScore, StringFormat='Distribution {0:F1}/25', Mode=OneWay}"/>
                                <ProgressBar Style="{StaticResource ScoreBar}" Value="{Binding DistributionScore}"/>
                            </StackPanel>
                        </Grid>
                        
                        <!-- Scoring Key -->
                        <Border Background="#F5F5F5" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                            <StackPanel>
                                <TextBlock Text="📖 Scoring Key" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,6"/>
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="#555" TextWrapping="Wrap" Margin="0,0,0,3"
                                         Text="Each dimension is scored 0–25. Total score = sum of all four (0–100)."
                                         FontStyle="Italic"/>
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="#555" TextWrapping="Wrap" Margin="0,0,0,2"
                                         Text="Completeness — Measures how few null/missing values exist. 0% nulls = 25/25."/>
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="#555" TextWrapping="Wrap" Margin="0,0,0,2"
                                         Text="Uniqueness — Measures value diversity. Higher distinct-value ratios score higher; constant columns score low."/>
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="#555" TextWrapping="Wrap" Margin="0,0,0,2"
                                         Text="Validity — Measures type consistency and value correctness. Penalizes empty strings, non-nullable columns with nulls, and suspicious patterns."/>
                                <TextBox Style="{StaticResource SelectableText}" FontSize="10" Foreground="#555" TextWrapping="Wrap"
                                         Text="Distribution — Measures data spread. Penalizes high outlier rates, extreme skew, and single-value dominance."/>
                                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                    <Ellipse Width="9" Height="9" Fill="#4CAF50" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBox Style="{StaticResource SelectableText}" Text="80–100 Good" FontSize="9" Foreground="#999" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <Ellipse Width="9" Height="9" Fill="#FF9800" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBox Style="{StaticResource SelectableText}" Text="60–79 Fair" FontSize="9" Foreground="#999" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <Ellipse Width="9" Height="9" Fill="#F44336" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBox Style="{StaticResource SelectableText}" Text="&lt;60 Needs Review" FontSize="9" Foreground="#999" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- Findings / Narrative Section -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock FontWeight="Bold" FontSize="13" Margin="0,0,0,6">
                            <Run Text="📋 Findings"/>
                            <Run Text="{Binding Findings.Count, StringFormat='({0})', Mode=OneWay}" Foreground="Gray"/>
                        </TextBlock>
                        
                        <!-- Severity filter chips -->
                        <WrapPanel Margin="0,0,0,8">
                            <Button Command="{Binding SetFindingFilterCommand}" CommandParameter="All"
                                    Padding="8,3" Margin="0,0,4,4" FontSize="10" Cursor="Hand"
                                    BorderThickness="1" BorderBrush="#CCC">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="#666"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedFindingFilter}" Value="All">
                                                <Setter Property="Background" Value="#512BD4"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#512BD4"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <TextBlock>
                                    <Run Text="All"/>
                                    <Run Text="{Binding Findings.Count, StringFormat=' ({0})', Mode=OneWay}"/>
                                </TextBlock>
                            </Button>
                            
                            <Button Command="{Binding SetFindingFilterCommand}" CommandParameter="Critical"
                                    Padding="8,3" Margin="0,0,4,4" FontSize="10" Cursor="Hand"
                                    BorderThickness="1" BorderBrush="#CCC">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="#666"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedFindingFilter}" Value="Critical">
                                                <Setter Property="Background" Value="#F44336"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#F44336"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="9" Height="9" Fill="#F44336" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock VerticalAlignment="Center">
                                        <Run Text="Needs Review"/>
                                        <Run Text="{Binding FindingsCriticalCount, StringFormat=' ({0})', Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Button>
                            
                            <Button Command="{Binding SetFindingFilterCommand}" CommandParameter="Warning"
                                    Padding="8,3" Margin="0,0,4,4" FontSize="10" Cursor="Hand"
                                    BorderThickness="1" BorderBrush="#CCC">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="#666"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedFindingFilter}" Value="Warning">
                                                <Setter Property="Background" Value="#FF9800"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#FF9800"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="9" Height="9" Fill="#FF9800" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock VerticalAlignment="Center">
                                        <Run Text="Fair"/>
                                        <Run Text="{Binding FindingsWarningCount, StringFormat=' ({0})', Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Button>
                            
                            <Button Command="{Binding SetFindingFilterCommand}" CommandParameter="Info"
                                    Padding="8,3" Margin="0,0,4,4" FontSize="10" Cursor="Hand"
                                    BorderThickness="1" BorderBrush="#CCC">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="White"/>
                                        <Setter Property="Foreground" Value="#666"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SelectedFindingFilter}" Value="Info">
                                                <Setter Property="Background" Value="#4CAF50"/>
                                                <Setter Property="Foreground" Value="White"/>
                                                <Setter Property="BorderBrush" Value="#4CAF50"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Ellipse Width="9" Height="9" Fill="#4CAF50" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBlock VerticalAlignment="Center">
                                        <Run Text="Good"/>
                                        <Run Text="{Binding FindingsInfoCount, StringFormat=' ({0})', Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Button>
                        </WrapPanel>
                        
                        <!-- Scrollable grouped findings -->
                        <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding GroupedFindings}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,4">
                                            <!-- Group header (toggle) -->
                                            <ToggleButton IsChecked="{Binding IsExpanded}"
                                                          Background="Transparent" BorderThickness="0"
                                                          HorizontalContentAlignment="Stretch"
                                                          Cursor="Hand" Padding="4,4">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Ellipse Width="9" Height="9" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                 Fill="{Binding Color, Converter={StaticResource StringToBrushConverter}}"/>
                                                        <TextBlock Text="{Binding Label}" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                    <Border Grid.Column="1" Background="#E8E8E8" CornerRadius="8"
                                                            Padding="6,1" VerticalAlignment="Center" Margin="6,0">
                                                        <TextBlock Text="{Binding Count}" FontSize="10" FontWeight="SemiBold" Foreground="#555"/>
                                                    </Border>
                                                    <TextBlock Grid.Column="2" FontSize="10" Foreground="#999" VerticalAlignment="Center" Margin="4,0,0,0">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Text" Value="▶"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                        <Setter Property="Text" Value="▼"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </Grid>
                                            </ToggleButton>
                                            
                                            <!-- Group items (visible when expanded) -->
                                            <ItemsControl ItemsSource="{Binding Items}">
                                                <ItemsControl.Style>
                                                    <Style TargetType="ItemsControl">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsExpanded}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </ItemsControl.Style>
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border Margin="16,0,0,3" Padding="8,4" CornerRadius="3"
                                                                BorderThickness="0,0,0,1">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="#FAFAFA"/>
                                                                    <Setter Property="BorderBrush" Value="#F0F0F0"/>
                                                                </Style>
                                                            </Border.Style>
                                                            <StackPanel>
                                                                <StackPanel Orientation="Horizontal">
                                                                    <Ellipse Width="7" Height="7" VerticalAlignment="Center" Margin="0,0,5,0"
                                                                             Fill="{Binding SeverityColor, Converter={StaticResource StringToBrushConverter}}"/>
                                                                    <TextBox Text="{Binding Title, Mode=OneWay}" FontWeight="SemiBold" FontSize="10"
                                                                             Style="{StaticResource SelectableText}"
                                                                             ToolTip="{Binding Title}"
                                                                             VerticalAlignment="Center"/>
                                                                </StackPanel>
                                                                <TextBox Text="{Binding Description, Mode=OneWay}" FontSize="9" Foreground="#888"
                                                                         TextWrapping="Wrap" Margin="12,1,0,0" MaxHeight="48"
                                                                         Style="{StaticResource SelectableText}"
                                                                         ToolTip="{Binding Description}"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </StackPanel>
                </Border>
                
                <!-- Filter / Query Builder -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="🔎 Filter Columns" FontWeight="Bold" FontSize="13" Margin="0,0,0,6"/>
                        <StackPanel Orientation="Horizontal">
                            <ComboBox ItemsSource="{Binding FilterableMetrics}" 
                                      SelectedItem="{Binding FilterColumn}"
                                      Width="100" Margin="0,0,4,0" FontSize="11"/>
                            <ComboBox SelectedValue="{Binding FilterOperator}" SelectedValuePath="Content" Width="45" Margin="0,0,4,0" FontSize="11">
                                <ComboBoxItem Content="&gt;"/>
                                <ComboBoxItem Content="&lt;"/>
                                <ComboBoxItem Content="="/>
                                <ComboBoxItem Content="&gt;="/>
                                <ComboBoxItem Content="&lt;="/>
                            </ComboBox>
                            <TextBox Text="{Binding FilterValue, UpdateSourceTrigger=PropertyChanged}" 
                                     Width="55" Margin="0,0,4,0" FontSize="11"
                                     VerticalContentAlignment="Center"/>
                            <Button Content="Go" Command="{Binding ApplyFilterCommand}" 
                                    Padding="8,2" FontSize="11" Margin="0,0,4,0"/>
                            <Button Content="✕" Command="{Binding ClearFilterCommand}" 
                                    Padding="6,2" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Column Profiles Section -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <!-- Header with toggle and sort -->
                        <Grid Margin="0,0,0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Button Command="{Binding ToggleColumnProfilesExpandedCommand}"
                                    Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left"
                                    Cursor="Hand" Padding="0">
                                <TextBlock FontWeight="Bold" FontSize="13">
                                    <Run Text="📊 Column Profiles"/>
                                    <Run Text="{Binding DisplayedColumns.Count, StringFormat='({0})', Mode=OneWay}" Foreground="Gray"/>
                                </TextBlock>
                            </Button>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                                <TextBlock Text="Sort:" FontSize="10" Foreground="#999" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                <ComboBox ItemsSource="{Binding ColumnSortOptions}"
                                          SelectedItem="{Binding ColumnSortBy}"
                                          FontSize="10" Width="80" Padding="4,2"/>
                            </StackPanel>
                            <Button Grid.Column="2" Command="{Binding ToggleColumnProfilesExpandedCommand}"
                                    Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="0"
                                    VerticalAlignment="Center">
                                <TextBlock FontSize="12" Foreground="#512BD4">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="▶"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsColumnProfilesExpanded}" Value="True">
                                                    <Setter Property="Text" Value="▼"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Button>
                        </Grid>

                        <!-- Table content (collapsible) -->
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsColumnProfilesExpanded}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <!-- Horizontal + Vertical scrollable table -->
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                                <StackPanel MinWidth="720">
                                    <!-- Table header -->
                                    <Border Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4,4,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="64"/>
                                                <ColumnDefinition Width="36"/>
                                                <ColumnDefinition Width="22"/>
                                                <ColumnDefinition Width="22"/>
                                                <ColumnDefinition Width="22"/>
                                                <ColumnDefinition Width="22"/>
                                                <ColumnDefinition Width="48"/>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="70"/>
                                                <ColumnDefinition Width="*" MinWidth="80"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="COLUMN" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="6,5"/>
                                            <TextBlock Grid.Column="1" Text="TYPE" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="3,5"/>
                                            <TextBlock Grid.Column="2" Text="SCR" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="2,5" HorizontalAlignment="Center"/>
                                            <TextBlock Grid.Column="3" Text="C" FontSize="9" FontWeight="Bold" Foreground="#4CAF50" Padding="0,5" HorizontalAlignment="Center" ToolTip="Completeness"/>
                                            <TextBlock Grid.Column="4" Text="U" FontSize="9" FontWeight="Bold" Foreground="#2196F3" Padding="0,5" HorizontalAlignment="Center" ToolTip="Uniqueness"/>
                                            <TextBlock Grid.Column="5" Text="V" FontSize="9" FontWeight="Bold" Foreground="#FF9800" Padding="0,5" HorizontalAlignment="Center" ToolTip="Validity"/>
                                            <TextBlock Grid.Column="6" Text="D" FontSize="9" FontWeight="Bold" Foreground="#9C27B0" Padding="0,5" HorizontalAlignment="Center" ToolTip="Distribution"/>
                                            <TextBlock Grid.Column="7" Text="NULLS" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="2,5"/>
                                            <TextBlock Grid.Column="8" Text="DIST" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="2,5" ToolTip="Distinct values"/>
                                            <TextBlock Grid.Column="9" Text="MIN" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="3,5"/>
                                            <TextBlock Grid.Column="10" Text="MAX" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="3,5"/>
                                            <TextBlock Grid.Column="11" Text="MEAN" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="3,5"/>
                                            <TextBlock Grid.Column="12" Text="DISTRIBUTION" FontSize="9" FontWeight="Bold" Foreground="#666" Padding="3,5"/>
                                        </Grid>
                                    </Border>

                                    <!-- Table rows -->
                                    <Border BorderBrush="#E0E0E0" BorderThickness="1,0,1,1" CornerRadius="0,0,4,4">
                                        <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
                                            <ItemsControl ItemsSource="{Binding DisplayedColumns}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="0,2">
                                                            <Border.Style>
                                                                <Style TargetType="Border">
                                                                    <Setter Property="Background" Value="White"/>
                                                                    <Style.Triggers>
                                                                        <Trigger Property="IsMouseOver" Value="True">
                                                                            <Setter Property="Background" Value="#FAFAFA"/>
                                                                        </Trigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Border.Style>
                                                            <Border.ToolTip>
                                                                <StackPanel MaxWidth="240">
                                                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                                    <TextBlock FontSize="11">
                                                                        <Run Text="Type: "/><Run Text="{Binding DuckDbType, Mode=OneWay}"/>
                                                                        <Run Text="  ·  "/><Run Text="{Binding Category, Mode=OneWay}"/>
                                                                    </TextBlock>
                                                                    <TextBlock FontSize="11">
                                                                        <Run Text="Completeness: "/><Run Text="{Binding Completeness, StringFormat={}{0:F1}%, Mode=OneWay}"/>
                                                                    </TextBlock>
                                                                    <TextBlock FontSize="11" Text="{Binding Median, StringFormat='Median: {0:G6}', TargetNullValue=''}"/>
                                                                    <TextBlock FontSize="11" Text="{Binding StdDev, StringFormat='StdDev: {0:G6}', TargetNullValue=''}"/>
                                                                    <TextBlock FontSize="11" Text="{Binding Q1, StringFormat='Q1: {0:G6}', TargetNullValue=''}"/>
                                                                    <TextBlock FontSize="11" Text="{Binding Q3, StringFormat='Q3: {0:G6}', TargetNullValue=''}"/>
                                                                    <TextBlock FontSize="11">
                                                                        <Run Text="Outliers: "/><Run Text="{Binding OutlierPercentage, StringFormat={}{0:F1}%, Mode=OneWay}"/>
                                                                    </TextBlock>
                                                                </StackPanel>
                                                            </Border.ToolTip>
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="120"/>
                                                                    <ColumnDefinition Width="64"/>
                                                                    <ColumnDefinition Width="36"/>
                                                                    <ColumnDefinition Width="22"/>
                                                                    <ColumnDefinition Width="22"/>
                                                                    <ColumnDefinition Width="22"/>
                                                                    <ColumnDefinition Width="22"/>
                                                                    <ColumnDefinition Width="48"/>
                                                                    <ColumnDefinition Width="50"/>
                                                                    <ColumnDefinition Width="70"/>
                                                                    <ColumnDefinition Width="70"/>
                                                                    <ColumnDefinition Width="70"/>
                                                                    <ColumnDefinition Width="*" MinWidth="80"/>
                                                                </Grid.ColumnDefinitions>

                                                                <!-- Column name -->
                                                                <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="10" FontWeight="SemiBold"
                                                                           Padding="6,2" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>

                                                                <!-- Type -->
                                                                <TextBlock Grid.Column="1" Text="{Binding DuckDbType}" FontSize="9" Foreground="#888"
                                                                           Padding="3,2" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>

                                                                <!-- Score badge -->
                                                                <Border Grid.Column="2" CornerRadius="8" Padding="3,1" Margin="1"
                                                                        Background="{Binding Score.BadgeBackground, Converter={StaticResource StringToBrushConverter}}"
                                                                        HorizontalAlignment="Center" VerticalAlignment="Center">
                                                                    <TextBlock Text="{Binding Score.Total, StringFormat={}{0:F0}}" FontSize="9" FontWeight="Bold"
                                                                               Foreground="{Binding Score.BadgeForeground, Converter={StaticResource StringToBrushConverter}}"
                                                                               HorizontalAlignment="Center"/>
                                                                </Border>

                                                                <!-- C U V D dimension scores -->
                                                                <TextBlock Grid.Column="3" Text="{Binding Score.Completeness, StringFormat={}{0:F0}}"
                                                                           FontSize="8" Foreground="#4CAF50" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="4" Text="{Binding Score.Uniqueness, StringFormat={}{0:F0}}"
                                                                           FontSize="8" Foreground="#2196F3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="5" Text="{Binding Score.Validity, StringFormat={}{0:F0}}"
                                                                           FontSize="8" Foreground="#FF9800" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="6" Text="{Binding Score.Distribution, StringFormat={}{0:F0}}"
                                                                           FontSize="8" Foreground="#9C27B0" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                                                <!-- Nulls -->
                                                                <TextBlock Grid.Column="7" FontSize="9" Foreground="#555" Padding="2,2" VerticalAlignment="Center">
                                                                    <Run Text="{Binding NullPercentage, StringFormat={}{0:F1}%, Mode=OneWay}"/>
                                                                </TextBlock>

                                                                <!-- Distinct -->
                                                                <TextBlock Grid.Column="8" Text="{Binding DistinctCount, StringFormat={}{0:N0}}"
                                                                           FontSize="9" Foreground="#555" Padding="2,2" VerticalAlignment="Center"/>

                                                                <!-- Min -->
                                                                <TextBlock Grid.Column="9" Text="{Binding DisplayMin}" FontSize="9" Foreground="#555"
                                                                           Padding="3,2" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                                                           ToolTip="{Binding DisplayMin}"/>

                                                                <!-- Max -->
                                                                <TextBlock Grid.Column="10" Text="{Binding DisplayMax}" FontSize="9" Foreground="#555"
                                                                           Padding="3,2" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                                                           ToolTip="{Binding DisplayMax}"/>

                                                                <!-- Mean -->
                                                                <TextBlock Grid.Column="11" Text="{Binding DisplayMean}" FontSize="9" Foreground="#555"
                                                                           Padding="3,2" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"
                                                                           ToolTip="{Binding DisplayMean}"/>

                                                                <!-- Distribution sparkline -->
                                                                <controls:SparklineControl Grid.Column="12" Buckets="{Binding Histogram}"
                                                                                           Height="20" Margin="3,1,3,1"
                                                                                           VerticalAlignment="Center"/>
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Border>
                                </StackPanel>
                            </ScrollViewer>
                        </StackPanel>
                    </StackPanel>
                </Border>
                
                <!-- Dimension Group-By Section -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <!-- Toggle header -->
                        <Button Command="{Binding ToggleGroupByExpandedCommand}" 
                                Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch"
                                Cursor="Hand" Padding="0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="📐 Group By Dimensions" FontWeight="Bold" FontSize="13"/>
                                <TextBlock Grid.Column="1" FontSize="12" Foreground="#512BD4" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="▶ Expand"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsGroupByExpanded}" Value="True">
                                                    <Setter Property="Text" Value="▼ Collapse"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Button>
                        
                        <!-- Expandable dimension picker -->
                        <StackPanel Margin="0,8,0,0">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGroupByExpanded}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            
                            <TextBlock Text="Select categorical columns to group by:" FontSize="10" Foreground="Gray" Margin="0,0,0,4"/>
                            
                            <ItemsControl ItemsSource="{Binding AvailableDimensions}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Name}" IsChecked="{Binding IsSelected}"
                                                  FontSize="11" Margin="0,0,10,4"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                <Button Content="▶ Apply Group By" Command="{Binding ApplyGroupByCommand}"
                                        Padding="10,4" FontSize="11" HorizontalAlignment="Left"
                                        Background="#512BD4" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                <Button Content="✕ Clear" Command="{Binding ClearGroupByCommand}"
                                        Padding="10,4" FontSize="11" Margin="8,0,0,0"
                                        Background="Transparent" Foreground="#999" BorderBrush="#CCC" BorderThickness="1" Cursor="Hand"/>
                            </StackPanel>
                            
                            <!-- Status message -->
                            <TextBlock Text="{Binding GroupByStatusMessage}" FontSize="10" Foreground="#888" Margin="0,4,0,0"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                        
                        <!-- Grouped Results (always visible when available, even when collapsed) -->
                        <ItemsControl ItemsSource="{Binding GroupedResults}" Margin="0,8,0,0"
                                      Visibility="{Binding HasGroupedResults, Converter={StaticResource BoolToVis}}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8F8F8" BorderBrush="#E0E0E0" BorderThickness="1"
                                            CornerRadius="4" Margin="0,0,0,4" Padding="8,6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding GroupKey}" FontSize="11" FontWeight="SemiBold"
                                                       TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="1" FontSize="10" Foreground="Gray" 
                                                       VerticalAlignment="Center" Margin="8,0">
                                                <Run Text="{Binding RowCount, StringFormat={}{0:N0}, Mode=OneWay}"/>
                                                <Run Text="rows"/>
                                            </TextBlock>
                                            <Border Grid.Column="2" CornerRadius="10" Padding="8,2"
                                                    VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#E8F5E9"/>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock FontSize="10" FontWeight="Bold"
                                                           Text="{Binding QualityScore, StringFormat={}{0:F0}}"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
                
                <!-- File Comparison Section -->
                <Border Style="{StaticResource SectionCard}"
                        Visibility="{Binding HasProfile, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="🔄 File Comparison" FontWeight="Bold" FontSize="13" Margin="0,0,0,6"/>
                        
                        <Button Content="Compare with another file..." 
                                Command="{Binding CompareWithFileCommand}"
                                Padding="10,4" FontSize="11" HorizontalAlignment="Left"/>
                        
                        <!-- Comparison results -->
                        <StackPanel Visibility="{Binding HasComparison, Converter={StaticResource BoolToVis}}"
                                    Margin="0,8,0,0">
                            <TextBlock FontSize="11" Margin="0,0,0,4">
                                <Run Text="Comparing with: " Foreground="Gray"/>
                                <Run Text="{Binding ComparisonFileName, Mode=OneWay}" FontWeight="SemiBold"/>
                            </TextBlock>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Margin="0,0,8,0">
                                    <TextBlock Text="Baseline" FontSize="10" Foreground="Gray"/>
                                    <TextBlock FontSize="11">
                                        <Run Text="{Binding Comparison.BaselineProfile.RowCount, StringFormat={}{0:N0}, Mode=OneWay}"/>
                                        <Run Text=" rows"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11">
                                        <Run Text="Score: "/>
                                        <Run Text="{Binding Comparison.BaselineProfile.OverallScore.Total, StringFormat={}{0:F1}, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Comparison" FontSize="10" Foreground="Gray"/>
                                    <TextBlock FontSize="11">
                                        <Run Text="{Binding Comparison.ComparisonProfile.RowCount, StringFormat={}{0:N0}, Mode=OneWay}"/>
                                        <Run Text=" rows"/>
                                    </TextBlock>
                                    <TextBlock FontSize="11">
                                        <Run Text="Score: "/>
                                        <Run Text="{Binding Comparison.ComparisonProfile.OverallScore.Total, StringFormat={}{0:F1}, Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </Grid>
                            
                            <TextBlock FontSize="11" Margin="0,6,0,0">
                                <Run Text="Row delta: " Foreground="Gray"/>
                                <Run Text="{Binding Comparison.RowCountDelta, StringFormat={}{0:+#,0;-#,0;0}, Mode=OneWay}"/>
                                <Run Text=" ("/>
                                <Run Text="{Binding Comparison.RowCountDeltaPercent, StringFormat={}{0:+0.0;-0.0;0.0}%, Mode=OneWay}"/>
                                <Run Text=")"/>
                            </TextBlock>
                            
                            <TextBlock FontSize="11" Margin="0,2,0,0">
                                <Run Text="Schema changes: " Foreground="Gray"/>
                                <Run Text="{Binding Comparison.SchemaChanges.Count, Mode=OneWay}"/>
                            </TextBlock>
                            
                            <TextBlock FontSize="11" Margin="0,2,0,0">
                                <Run Text="Drift score: " Foreground="Gray"/>
                                <Run Text="{Binding Comparison.DriftScore, StringFormat={}{0:F1}/100, Mode=OneWay}"/>
                            </TextBlock>

                            <!-- Schema Diff Table -->
                            <Expander Header="📊 Schema Diff" Margin="0,8,0,0" FontSize="12" FontWeight="SemiBold"
                                      Visibility="{Binding HasSchemaDiff, Converter={StaticResource BoolToVis}}"
                                      IsExpanded="True">
                                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Margin="0,4,0,0">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <!-- Header row -->
                                        <Grid Background="#F5F5F5" Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="" FontSize="10" FontWeight="Bold" Padding="4,3"/>
                                            <TextBlock Grid.Column="1" Text="Column" FontSize="10" FontWeight="Bold" Padding="4,3"/>
                                            <TextBlock Grid.Column="2" Text="Baseline" FontSize="10" FontWeight="Bold" Padding="4,3" Foreground="#1565C0"/>
                                            <TextBlock Grid.Column="3" Text="Compare" FontSize="10" FontWeight="Bold" Padding="4,3" Foreground="#6A1B9A"/>
                                        </Grid>
                                        <!-- Diff rows -->
                                        <ItemsControl Grid.Row="1" ItemsSource="{Binding SchemaDiffItems}" MaxHeight="200">
                                            <ItemsControl.Template>
                                                <ControlTemplate>
                                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                                        <ItemsPresenter/>
                                                    </ScrollViewer>
                                                </ControlTemplate>
                                            </ItemsControl.Template>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="24"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Grid.Column="0" Text="{Binding StatusIcon}" 
                                                                   FontWeight="Bold" FontSize="12" Padding="6,2"
                                                                   Foreground="{Binding StatusColor, Converter={StaticResource StringToBrushConverter}}"
                                                                   HorizontalAlignment="Center"/>
                                                        <TextBlock Grid.Column="1" Text="{Binding ColumnName}" 
                                                                   FontSize="10" Padding="4,2" TextTrimming="CharacterEllipsis"
                                                                   ToolTip="{Binding ColumnName}"/>
                                                        <TextBlock Grid.Column="2" Text="{Binding BaselineType}" 
                                                                   FontSize="10" Padding="4,2" Foreground="#666"/>
                                                        <TextBlock Grid.Column="3" Text="{Binding ComparisonType}" 
                                                                   FontSize="10" Padding="4,2" Foreground="#666"/>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Grid>
                                </Border>
                            </Expander>
                        </StackPanel>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
